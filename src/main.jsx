import React from 'react'
import ReactDOM from 'react-dom/client'

import DataComponent from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DataComponent />
  </React.StrictMode>,
)
